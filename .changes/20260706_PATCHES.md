# Two small edits to existing files

## 1. src/agent/orchestrator.ts — describeStrategy()

Add a case for the new tool so verbose output labels it correctly:

```ts
function describeStrategy(toolName: string): string {
  switch (toolName) {
    case "index_lookup_tool":
      return "search phase — checking the precomputed workspace index before touching the filesystem";
    case "dump_read_tool":
      return "search phase — digesting the precomputed full-content dump (.devx/index.dump) as the authoritative source of truth";
    case "glob_tool":
      ...
```

(Just insert the `dump_read_tool` case; nothing else in this file changes. Bump the `@version` header.)

## 2. src/agent/promptBuilder.ts — buildSystemPrompt() RULES section

Replace this line:

```
- If a workspace .devx/index.dump exists, try read_tool, this contain all files content in current workspace.
```

with:

```
- If a workspace .devx/index.dump exists, use dump_read_tool ("list" then "read") instead of read_tool to
  digest its full content — it returns one complete file per call instead of arbitrary line windows that
  can split a file's content across calls or straddle two unrelated files.
```

(Bump the `@version` header.)
